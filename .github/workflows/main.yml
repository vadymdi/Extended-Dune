name: Extended Exchange Data Collection

on:
  schedule:
    # Запускаємо кожні 1 годин
    - cron: '0 */1 * * *'
  workflow_dispatch:
    # Можна запустити вручну
  push:
    branches: [ main ]
    paths: 
      - 'enhanced_fetch_data_to_dune.py'

jobs:
  collect-data:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Set up Python 3.10
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        cache: 'pip'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: Run enhanced data collection
      run: |
        echo "🚀 Starting enhanced data collection..."
        python fetch_data_to_dune.py
        echo "✅ Data collection completed"
        
    - name: Check collected data
      run: |
        echo "📊 Data collection results:"
        ls -la uploads/ || echo "uploads/ directory not found"
        
        for file in uploads/*.csv; do
          if [ -f "$file" ]; then
            echo "📄 $(basename "$file"): $(wc -l < "$file") rows"
          fi
        done
        
    - name: Upload to Dune Analytics
      env:
        DUNE_API_KEY: ${{ secrets.DUNE_API_KEY }}
      run: |
        echo "⬆️ Uploading to Dune Analytics..."
        python scripts/upload_to_dune.py
        echo "✅ Upload completed"
